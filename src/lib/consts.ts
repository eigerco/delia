// NOTE: ideally, this module will be gone once we get the ports issue settled
export const DEFAULT_LOCAL_STORAGE_ADDRESS = { ip: "127.0.0.1", port: 8000 };
export const DEFAULT_LOCAL_RPC_ADDRESS = { ip: "127.0.0.1", port: 8001 };
